{
  "name": "tsop-testing",
  "version": "0.1.0",
  "type": "module",
  "description": "TSOP test framework: Bitcoin Script VM, reference interpreter, and fuzzer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "files": ["dist"],
  "dependencies": {
    "tsop-lang": "workspace:*",
    "tsop-ir-schema": "workspace:*",
    "tsop-compiler": "workspace:*",
    "@bsv/sdk": "^2.0.5"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "fast-check": "^3.22.0"
  }
}
