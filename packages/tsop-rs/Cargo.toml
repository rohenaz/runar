[package]
name = "tsop"
version = "0.1.0"
edition = "2021"
description = "Types and mock functions for TSOP smart contract development in Rust"

[dependencies]
tsop-macros = { path = "../tsop-rs-macros" }
tsop-compiler-rust = { path = "../../compilers/rust" }
sha2 = "0.10"
ripemd = "0.1"
